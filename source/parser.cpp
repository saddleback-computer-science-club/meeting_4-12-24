#include "application.h"

/* Parses the data in specified file (`required_course.txt` for now),
    - Returns a vector of university's data as was found in the file
*/
std::vector<University> parseFile(std::string_view fileName)
{
    // ...
    return {};
}